This directory exists to be populated with rendered images. You should use a
real server to server the images out of this directory; if you don't, the
express staticfiles middleware will do it instead.
